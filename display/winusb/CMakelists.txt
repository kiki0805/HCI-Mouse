cmake_minimum_required(VERSION 3.0)
link_directories(${CMAKE_CURRENT_SOURCE_DIR}/3rdParty/MS64)

add_executable(testusb test.cpp)
target_link_libraries(testusb libusb-1.0)

# This is for dll moving.
# add_custom_command(TARGET testusb POST_BUILD
#     COMMAND ${CMAKE_COMMAND} -E copy_directory
#         "${CMAKE_CURRENT_SOURCE_DIR}/3rdParty/MS64"
#         $<TARGET_FILE_DIR:testusb>)